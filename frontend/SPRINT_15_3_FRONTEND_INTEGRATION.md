# Integracao frontend da Sprint 15.3

O modulo esta em `src/features/instrumentGovernance`.

```tsx
import { instrumentGovernanceRoutes } from './features/instrumentGovernance';
const routes = [...rotasExistentes, ...instrumentGovernanceRoutes];
```

Rotas: `/admin/instrument-governance` e `/teacher/instrument-results`.
