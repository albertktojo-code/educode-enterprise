# Integracao frontend da Sprint 15

O modulo esta em `src/features/assessmentHub`.

```tsx
import { assessmentHubRoutes } from './features/assessmentHub';
const routes = [...rotasExistentes, ...assessmentHubRoutes];
```

Rotas: `/teacher/assessment-hub` e `/admin/assessment-hub`.
