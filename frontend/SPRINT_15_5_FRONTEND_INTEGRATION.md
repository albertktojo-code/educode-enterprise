# Integracao frontend da Sprint 15.5

O modulo esta em `src/features/assessmentAnalytics`.

```tsx
import { assessmentAnalyticsRoutes } from './features/assessmentAnalytics';
const routes = [...rotasExistentes, ...assessmentAnalyticsRoutes];
```

Rotas: `/teacher/assessment-analytics`, `/teacher/assessment-analytics/items`, `/teacher/assessment-analytics/skills` e `/admin/assessment-analytics/reports`.
