# Integracao frontend da Sprint 15.4

O modulo esta em `src/features/assessmentReview`.

```tsx
import { assessmentReviewRoutes } from './features/assessmentReview';
const routes = [...rotasExistentes, ...assessmentReviewRoutes];
```

Rotas: `/teacher/assessment-review`, `/admin/assessment-rubrics` e `/teacher/assessment-appeals`.
