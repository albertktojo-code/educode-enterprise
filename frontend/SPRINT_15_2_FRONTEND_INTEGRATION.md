# Integracao frontend da Sprint 15.2

O modulo esta em `src/features/assessmentDelivery`.

```tsx
import { assessmentDeliveryRoutes } from './features/assessmentDelivery';
const routes = [...rotasExistentes, ...assessmentDeliveryRoutes];
```

Rotas: `/teacher/assessment-delivery`, `/admin/assessment-delivery` e `/student/assessments`.
