# Integração das rotas da Sprint 14.2

O módulo está em `src/features/adaptiveInsights`.

```tsx
import { adaptiveInsightsRoutes } from './features/adaptiveInsights';

const routes = [
  ...rotasExistentes,
  ...adaptiveInsightsRoutes,
];
```

Entrada docente: `/teacher/adaptive-insights`.
